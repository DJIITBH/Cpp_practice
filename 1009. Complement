class Solution {
public:
    int bitwiseComplement(int n) {
        int i=0, ans=0,x=0;
        x=n;
        while(n!=0)
        {
            n=n/2;
            i++;
        }
        ans = pow(2,i) - x - 1;
        if(x==0)
        {
            return 1;
        }
        return ans;
    }
};
